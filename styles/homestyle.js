import { StyleSheet } from 'react-native';

export const homeCSS = StyleSheet.create({
  container: {
    backgroundColor: "#171717",
    flex: 1,
    paddingTop: 50,
    alignItems: "center"
  },

  // TODO: Remove this. It was only for an example
  text: {
    color: "#E5E5E5",
    paddingBottom: 50,
    fontSize: 30
  }
})